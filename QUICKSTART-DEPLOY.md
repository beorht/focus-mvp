# ⚡ Быстрый деплой на Vercel (5 минут)

## Шаг 1: Подготовка (локально)

```bash
# Проверьте, что проект собирается
npm run build

# Если ошибок нет - всё готово!
```

## Шаг 2: Загрузка на GitHub

```bash
# Инициализируйте Git (если ещё не сделали)
git init
git add .
git commit -m "Ready for hackathon deployment"

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ваш-username/focus-mvp.git
git branch -M main
git push -u origin main
```

## Шаг 3: Деплой на Vercel

### 3.1 Регистрация
1. Перейдите на https://vercel.com
2. Нажмите **Sign Up**
3. Выберите **Continue with GitHub**
4. Авторизуйте Vercel

### 3.2 Импорт проекта
1. На главной странице Vercel нажмите **Add New** → **Project**
2. В списке найдите репозиторий `focus-mvp`
3. Нажмите **Import**

### 3.3 Настройка
```
Framework Preset: Next.js ✅ (автоматически определится)
Root Directory: ./
Build Command: npm run build ✅ (автоматически)
Output Directory: .next ✅ (автоматически)
Install Command: npm install ✅ (автоматически)
```

### 3.4 Environment Variables (ВАЖНО!)
Перед деплоем добавьте переменную окружения:

```
Name:  GEMINI_API_KEY
Value: ваш_ключ_из_.env.local
```

**Где взять ключ?**
```bash
cat .env.local
# Скопируйте значение после GEMINI_API_KEY=
```

### 3.5 Deploy
1. Нажмите **Deploy**
2. Ждите 2-3 минуты
3. ✅ Готово!

## Шаг 4: Получение ссылки

После успешного деплоя вы получите:
```
https://focus-mvp-ваш-username.vercel.app
```

**Важные страницы для хакатона:**
- Главная: `https://your-project.vercel.app/`
- Демо: `https://your-project.vercel.app/demo` ← Эту ссылку отправляйте жюри!

## 🔄 Автоматические обновления

После первого деплоя:
```bash
# Внесите изменения в код
git add .
git commit -m "Update feature"
git push

# Vercel автоматически задеплоит новую версию!
```

## ✅ Checklist после деплоя

Откройте свой сайт и проверьте:

- [ ] Главная страница загружается
- [ ] Кнопка "Start Journey" работает
- [ ] Можно пройти все 5 вопросов
- [ ] AI анализ запускается (крутилка)
- [ ] Результаты отображаются
- [ ] Chat-ассистент отвечает на вопросы
- [ ] Правая панель (Terminal) показывает логи
- [ ] Страница `/demo` доступна

## 🆘 Если что-то не работает

### Ошибка: "API key not found"
```bash
# Проверьте переменные окружения в Vercel:
# Settings → Environment Variables → GEMINI_API_KEY
```

### Ошибка: "Build failed"
```bash
# Проверьте локально:
npm install
npm run build

# Если локально работает - проверьте логи в Vercel
```

### AI не отвечает
1. Проверьте Gemini API Key в настройках Vercel
2. Убедитесь, что у ключа есть квота
3. Проверьте Network tab в браузере (F12)

## 📊 Мониторинг

После деплоя в Vercel Dashboard доступно:
- **Analytics** - статистика посещений
- **Logs** - логи API routes в реальном времени
- **Deployments** - история всех деплоев

## 🎯 Для хакатона

**Что отправить жюри:**
```
Ссылка на демо: https://focus-mvp-username.vercel.app/demo
GitHub: https://github.com/username/focus-mvp
```

**Deadline:** 7 декабря 2024, 23:59 (GMT+5)

---

**Готово! Ваш проект теперь доступен онлайн 🚀**

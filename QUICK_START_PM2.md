# 🚀 Быстрый запуск FOCUS MVP с PM2

## Шаг 1: Подготовка (на локальной машине)

```bash
# Убедитесь что проект собирается
npm run build

# Закоммитьте все изменения
git add .
git commit -m "Add PM2 deployment configuration"
git push origin main
```

## Шаг 2: Деплой на сервер

### На сервере (VPS/Cloud):

```bash
# 1. Клонируйте проект
git clone <your-repo-url> focus-mvp
cd focus-mvp

# 2. Установите зависимости
npm install

# 3. Создайте .env.local с вашими API ключами
nano .env.local
# Вставьте:
# GEMINI_API_KEYS=key1,key2,key3
# GEMINI_API_KEYS_FOR_CHAT_ASSITENT=key1,key2,key3

# 4. Запустите автоматический деплой
chmod +x deploy.sh
./deploy.sh
```

**Готово!** 🎉 Приложение запущено на порту 3000

## Проверка статуса

```bash
pm2 status
```

Вывод должен быть:
```
┌─────┬──────────────┬─────────────┬─────────┬─────────┬──────────┬
│ id  │ name         │ namespace   │ version │ mode    │ pid      │
├─────┼──────────────┼─────────────┼─────────┼─────────┼──────────┼
│ 0   │ focus-mvp    │ default     │ N/A     │ fork    │ 12345    │
└─────┴──────────────┴─────────────┴─────────┴─────────┴──────────┴
```

## Основные команды

```bash
# Логи в реальном времени
npm run pm2:logs

# Перезапуск приложения
npm run pm2:restart

# Остановка
npm run pm2:stop

# Мониторинг
npm run pm2:monit

# Статус
npm run pm2:status
```

## Обновление приложения

```bash
git pull origin main
./deploy.sh
```

## Настройка автозапуска при перезагрузке сервера

После первого запуска выполните команду, которую покажет PM2:

```bash
# Пример команды (команда будет показана при деплое):
sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u username --hp /home/username
```

Затем сохраните конфигурацию:

```bash
pm2 save
```

## Nginx (опционально - для доменного имени)

Создайте `/etc/nginx/sites-available/focus-mvp`:

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Активируйте:

```bash
sudo ln -s /etc/nginx/sites-available/focus-mvp /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

## Устранение проблем

**Приложение не запускается?**
```bash
# Проверьте логи
npm run pm2:logs

# Проверьте не занят ли порт
lsof -i :3000
```

**PM2 команды не работают?**
```bash
# Установите PM2 глобально
npm install -g pm2
```

**Изменить порт?**
Отредактируйте `ecosystem.config.js` → `env.PORT`

---

📖 **Полная документация:** [DEPLOYMENT.md](./DEPLOYMENT.md)

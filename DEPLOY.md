# 🚀 Инструкция по деплою F.O.C.U.S

## Вариант 1: Vercel (Рекомендуется) ⭐

### Преимущества:
- ✅ Автоматический деплой из GitHub
- ✅ Бесплатный HTTPS
- ✅ Глобальный CDN
- ✅ Простая настройка переменных окружения
- ✅ Оптимизировано для Next.js

### Шаги:

1. **Залейте код на GitHub:**
```bash
git add .
git commit -m "Ready for deployment"
git push origin main
```

2. **Зарегистрируйтесь на Vercel:**
   - Перейдите на https://vercel.com
   - Войдите через GitHub аккаунт

3. **Создайте новый проект:**
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий `focus-mvp`
   - Vercel автоматически определит Next.js

4. **Настройте переменные окружения:**
   - В настройках проекта → Environment Variables
   - Добавьте: `GEMINI_API_KEY` = `ваш_ключ_из_.env.local`

5. **Деплой:**
   - Нажмите "Deploy"
   - Через 2-3 минуты получите ссылку типа: `https://focus-mvp.vercel.app`

### Автоматические обновления:
После первого деплоя каждый `git push` автоматически деплоит новую версию!

---

## Вариант 2: Build для Node.js хостинга

Если используете VPS (DigitalOcean, AWS, Heroku и т.д.):

### Локальная сборка:
```bash
# 1. Установить зависимости
npm install

# 2. Собрать проект
npm run build

# 3. Запустить production сервер
npm start
```

### Что загружать на хостинг:
```
.next/              (результат npm run build)
public/             (статические файлы)
node_modules/       (или npm install на сервере)
package.json
package-lock.json
next.config.ts
.env.local          (с вашими ключами)
```

### Команды на сервере:
```bash
npm install --production
npm run build
npm start
```

Сервер запустится на порту 3000 (настраивается через `PORT` env variable).

---

## Вариант 3: Docker (для продвинутых)

### Создайте `Dockerfile`:
```dockerfile
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
```

### Команды:
```bash
docker build -t focus-mvp .
docker run -p 3000:3000 -e GEMINI_API_KEY=your_key focus-mvp
```

---

## Вариант 4: Netlify

### Шаги:
1. Подключите GitHub репозиторий к Netlify
2. Build command: `npm run build`
3. Publish directory: `.next`
4. Добавьте переменную окружения `GEMINI_API_KEY`

**Внимание:** Netlify требует дополнительной настройки для Next.js API routes.

---

## ⚠️ Важные моменты перед деплоем:

### 1. Проверьте .env.local
Убедитесь, что API ключ Gemini валидный:
```bash
cat .env.local
```

### 2. Проверьте .gitignore
`.env.local` НЕ должен попасть в Git:
```bash
git status
# .env.local не должен быть в списке
```

### 3. Тестовый билд локально:
```bash
npm run build
npm start
# Откройте http://localhost:3000
```

### 4. Переменные окружения на production:
На Vercel/хостинге создайте переменную:
- **Ключ:** `GEMINI_API_KEY`
- **Значение:** ваш API ключ

---

## 🎯 Для хакатона - Быстрый старт:

```bash
# 1. Коммит изменений
git add .
git commit -m "Final version for hackathon"
git push

# 2. Деплой на Vercel (через UI)
# - Зайти на vercel.com
# - Import Git Repository
# - Добавить GEMINI_API_KEY
# - Deploy

# 3. Получить ссылку для демо страницы:
# https://your-project.vercel.app/demo
```

---

## 📊 Проверка после деплоя:

1. ✅ Главная страница загружается
2. ✅ Кнопка "Start Journey" работает
3. ✅ Тестирование проходит корректно
4. ✅ AI анализ возвращает результаты
5. ✅ Chat-ассистент отвечает
6. ✅ LogConsole показывает логи
7. ✅ Страница `/demo` доступна

---

## 🆘 Troubleshooting:

**Ошибка: "API key not found"**
- Проверьте переменные окружения на хостинге
- Название должно быть точно `GEMINI_API_KEY`

**Ошибка: "Module not found"**
```bash
npm install
npm run build
```

**Стили не загружаются:**
- Проверьте, что `tailwindcss` в dependencies (не в devDependencies)

**API routes не работают:**
- Убедитесь, что хостинг поддерживает серверные функции (Vercel, Node.js hosting)
- Static export НЕ поддерживает API routes

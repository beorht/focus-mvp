# Обновление контрастности страницы теста

## ✅ Обновлено (2025-12-13)

Все текстовые элементы и кнопки на странице теста обновлены для максимальной контрастности с новым темно-зеленым фоном светлой темы.

## 🎨 Изменения цветов

### 1. Фон карточки вопроса

**Было:**
```tsx
style={{ background: theme === 'dark' ? '#1d1d1d' : '#EADFD7' }}
// Светлый бежевый - низкий контраст с темными цветами
```

**Стало:**
```tsx
style={{ background: theme === 'dark' ? '#1d1d1d' : '#405952' }}
// Темно-зеленый из палитры --card-bg
```

### 2. Прогресс бар

**Было:**
```tsx
// Фон: bg-gray-300 (светло-серый)
// Заполнение: from-blue-600 to-purple-600 (синий-фиолетовый градиент)
```

**Стало:**
```tsx
// Фон: bg-[#4a6660] (темно-зеленый)
// Заполнение: from-[#8eb69b] to-[#5a8f6d] (зеленый градиент)
```

### 3. Текст "Вопрос X из Y"

**Было:**
```tsx
theme === 'dark' ? 'text-gray-300' : 'text-gray-700'
// text-gray-700 не видно на темно-зеленом
```

**Стало:**
```tsx
theme === 'dark' ? 'text-gray-100' : 'text-white'
// Белый текст - максимальная контрастность
```

### 4. Процент прогресса

**Было:**
```tsx
className="text-sm font-medium text-blue-400"
// Одинаковый для обеих тем
```

**Стало:**
```tsx
theme === 'dark' ? 'text-blue-400' : 'text-[#8eb69b]'
// Зеленый акцент для светлой темы
```

### 5. Заголовок вопроса (h2)

**Было:**
```tsx
theme === 'dark' ? 'text-white' : 'text-gray-900'
// text-gray-900 плохо видно
```

**Стало:**
```tsx
theme === 'dark' ? 'text-white' : 'text-white'
// Белый для обеих тем
```

### 6. Описание типа вопроса

**Было:**
```tsx
// "Можно выбрать несколько вариантов"
theme === 'dark' ? 'text-white' : 'text-gray-700'
```

**Стало:**
```tsx
theme === 'dark' ? 'text-white' : 'text-white'
```

### 7. Шкала Ликерта - инструкция

**Было:**
```tsx
theme === 'dark' ? 'text-gray-400' : 'text-gray-600'
```

**Стало:**
```tsx
theme === 'dark' ? 'text-gray-400' : 'text-white'
```

### 8. Шкала Ликерта - метки "Не согласен" / "Полностью согласен"

**Было:**
```tsx
theme === 'dark' ? 'text-gray-500' : 'text-gray-600'
```

**Стало:**
```tsx
theme === 'dark' ? 'text-gray-500' : 'text-white'
```

### 9. Шкала Ликерта - кнопки (1-5)

**Было:**
```tsx
// Не выбрано:
theme === 'dark'
  ? 'border-gray-700 hover:border-gray-600 text-gray-400 hover:text-gray-300'
  : 'border-gray-300 hover:border-gray-400 text-gray-600 hover:text-gray-700'

// Выбрано:
'bg-gradient-to-br from-blue-600 to-purple-600 border-transparent text-white shadow-lg'
```

**Стало:**
```tsx
// Не выбрано:
theme === 'dark'
  ? 'border-gray-700 hover:border-gray-600 text-gray-400 hover:text-gray-300'
  : 'border-white hover:border-[#8eb69b] text-white hover:text-[#8eb69b]'

// Выбрано:
'bg-gradient-to-br from-[#8eb69b] to-[#5a8f6d] border-transparent text-white shadow-lg'
```

### 10. Шкала Ликерта - номера под кнопками

**Было:**
```tsx
theme === 'dark' ? 'text-gray-600' : 'text-gray-500'
```

**Стало:**
```tsx
theme === 'dark' ? 'text-gray-600' : 'text-white/70'
```

### 11. Текстовое поле (textarea)

**Было:**
```tsx
theme === 'dark'
  ? 'bg-gray-800 border-gray-700 text-white placeholder-gray-500 focus:border-blue-500'
  : 'bg-white border-gray-300 text-gray-900 placeholder-gray-400 focus:border-blue-500'
```

**Стало:**
```tsx
theme === 'dark'
  ? 'bg-gray-800 border-gray-700 text-white placeholder-gray-500 focus:border-blue-500'
  : 'bg-[#405952] border-white text-white placeholder-white/50 focus:border-[#8eb69b]'
// + focus:ring-[#8eb69b]/20 для обеих тем
```

### 12. Счетчик символов

**Было:**
```tsx
theme === 'dark' ? 'text-gray-500' : 'text-gray-600'
```

**Стало:**
```tsx
theme === 'dark' ? 'text-gray-500' : 'text-white/70'
```

### 13. Опции вопросов - рамка

**Было:**
```tsx
// Не выбрано:
theme === 'dark' ? 'border-gray-700 hover:border-gray-600' : 'border-gray-300 hover:border-gray-400'

// Выбрано:
theme === 'dark' ? 'bg-blue-900/20 border-blue-900' : 'bg-blue-100 border-blue-400'
```

**Стало:**
```tsx
// Не выбрано:
theme === 'dark' ? 'border-gray-700 hover:border-gray-600' : 'border-white hover:border-[#8eb69b]'

// Выбрано:
theme === 'dark' ? 'bg-blue-900/20 border-blue-900' : 'bg-[#6a9d7d]/30 border-[#8eb69b]'
```

### 14. Опции вопросов - текст

**Было:**
```tsx
// Не выбрано:
theme === 'dark' ? 'text-gray-300' : 'text-gray-700'

// Выбрано:
theme === 'dark' ? 'text-blue-300' : 'text-blue-700'
```

**Стало:**
```tsx
// Не выбрано:
theme === 'dark' ? 'text-gray-300' : 'text-white'

// Выбрано:
theme === 'dark' ? 'text-blue-300' : 'text-white'
```

### 15. Опции вопросов - иконка галочки

**Было:**
```tsx
<div className="w-6 h-6 bg-blue-600 rounded-full flex items-center justify-center">
  <Check className="w-4 h-4 text-white" />
</div>
```

**Стало:**
```tsx
<div className={`w-6 h-6 rounded-full flex items-center justify-center ${
  theme === 'dark' ? 'bg-blue-600' : 'bg-[#8eb69b]'
}`}>
  <Check className="w-4 h-4 text-white" />
</div>
```

### 16. Кнопка "Назад"

**Было:**
```tsx
// Disabled:
'text-gray-600 cursor-not-allowed'

// Enabled:
theme === 'dark' ? 'text-gray-300 hover:bg-gray-800' : 'text-gray-700 hover:bg-gray-200'
```

**Стало:**
```tsx
// Disabled:
theme === 'dark' ? 'text-gray-600 cursor-not-allowed' : 'text-white/40 cursor-not-allowed'

// Enabled:
theme === 'dark' ? 'text-gray-300 hover:bg-gray-800' : 'text-white hover:bg-[#4a6660]'
```

### 17. Кнопка "Далее" / "Завершить"

**Было:**
```tsx
// Disabled:
theme === 'dark' ? 'bg-gray-800 text-gray-600 cursor-not-allowed' : 'bg-gray-300 text-gray-500 cursor-not-allowed'

// Enabled:
'bg-gradient-to-r from-blue-600 to-purple-600 text-white hover:shadow-lg hover:scale-105'

// Hover shadow:
boxShadow: "0 10px 30px rgba(59, 130, 246, 0.3)"
```

**Стало:**
```tsx
// Disabled:
theme === 'dark' ? 'bg-gray-800 text-gray-600 cursor-not-allowed' : 'bg-[#4a6660] text-white/40 cursor-not-allowed'

// Enabled:
'bg-gradient-to-r from-[#8eb69b] to-[#5a8f6d] text-white hover:shadow-lg hover:scale-105'

// Hover shadow:
boxShadow: "0 10px 30px rgba(142, 182, 155, 0.3)"
```

## 📊 Визуальное сравнение

### До (светлая тема)

```
Фон страницы: Темно-зеленый #2d4a45
Карточка вопроса: Светлый бежевый #EADFD7 ❌ (низкий контраст)
Текст вопроса: text-gray-700 ❌ (плохо видно)
Прогресс бар: Синий/фиолетовый ❌ (не соответствует теме)
Кнопка "Назад": text-gray-700 ❌ (почти невидима)
Кнопка "Далее": Синий/фиолетовый градиент ❌
```

### После (светлая тема)

```
Фон страницы: Темно-зеленый #2d4a45
Карточка вопроса: Темно-зеленый #405952 ✅ (гармонично)
Текст вопроса: text-white ✅ (отлично видно)
Прогресс бар: Зеленый градиент ✅ (соответствует теме)
Кнопка "Назад": text-white ✅ (отлично видно)
Кнопка "Далее": Зеленый градиент ✅
```

## 🎨 Цветовая палитра (светлая тема)

| Элемент | Цвет | Hex |
|---------|------|-----|
| Фон карточки | Темно-зеленый | `#405952` |
| Текст основной | Белый | `#ffffff` |
| Текст приглушенный | Белый 70% | `rgba(255,255,255,0.7)` |
| Акцент (выбранное) | Светло-зеленый | `#8eb69b` |
| Градиент кнопок | От `#8eb69b` до `#5a8f6d` | - |
| Фон при наведении | Приглушенный зеленый | `#4a6660` |
| Рамки | Белый | `#ffffff` |
| Рамки hover | Светло-зеленый | `#8eb69b` |

## ✅ Преимущества

1. **Максимальная контрастность** - белый текст на темно-зеленом фоне
2. **Единая цветовая схема** - все элементы используют зеленую палитру
3. **Видимость кнопки "Назад"** - теперь четко видна в светлой теме
4. **Согласованность** - прогресс бар и кнопки соответствуют общей теме
5. **Accessibility** - отличная читаемость для всех пользователей
6. **Профессиональный вид** - спокойная зеленая гамма

## 🧪 Тестирование

1. Запустите dev сервер:
```bash
npm run dev
```

2. Откройте http://localhost:3000/test

3. Переключите на светлую тему (☀️)

4. Проверьте:
   - **Прогресс бар** - зеленый градиент, белый текст
   - **Заголовок вопроса** - белый, четко читается
   - **Шкала Ликерта** - белые кнопки с зеленым при выборе
   - **Текстовое поле** - темно-зеленый фон, белый текст
   - **Кнопка "Назад"** - белая, видна четко
   - **Кнопка "Далее"** - зеленый градиент

5. Проверьте темную тему (🌙) - все должно работать как раньше

## 📝 Файлы изменены

- `app/test/page.tsx` - все текстовые элементы, кнопки, фоны

## 🎯 Результат

Страница теста теперь имеет отличную контрастность в светлой теме:
- Все тексты белые и четко читаются
- Кнопки используют зеленую палитру
- Фон карточки гармонирует с общей темой
- Кнопка "Назад" четко видна
- Прогресс бар соответствует цветовой схеме

Проблема с невидимостью текста полностью решена! ✨

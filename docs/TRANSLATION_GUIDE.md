# Руководство по использованию функции перевода

## ✅ Исправления (2025-12-13)

Функция перевода теперь работает корректно! Были исправлены следующие проблемы:

1. ✅ Добавлен компонент **LanguageSwitcher** на страницу урока
2. ✅ Исправлена логика useEffect для автоматического перевода
3. ✅ Улучшена обработка зависимостей в React hooks

## Как использовать перевод

### 1. На странице урока (/lesson)

В правом верхнем углу sidebar есть переключатель языка:

```
[← К результатам]    [🌐 Русский ▼]
```

При клике открывается меню с тремя языками:
- 🇷🇺 Русский (Russian)
- 🇺🇿 Ўзбекча (Uzbek Cyrillic)
- 🇺🇿 O'zbekcha (Uzbek Latin)

### 2. Что переводится автоматически

При смене языка переводятся все учебные материалы:
- ✅ Название профессии
- ✅ Заголовки тем
- ✅ Описания тем (summary)
- ✅ Примеры (examples)
- ✅ Задания (tasks)
- ✅ Вопросы квизов (quiz)

### 3. Индикатор перевода

Во время перевода в правом верхнем углу контента появляется индикатор:

```
[⟳ Перевод...]
```

Это означает, что система:
1. Отправляет запрос к Gemini API
2. Переводит весь контент урока
3. Обновляет интерфейс

## Технические детали

### Архитектура

```
User меняет язык
    ↓
LanguageSwitcher обновляет useLanguageStore
    ↓
useEffect в lesson/page.tsx обнаруживает изменение
    ↓
translateContent() вызывает /api/translate
    ↓
Gemini API переводит JSON
    ↓
Обновляется lessonData
    ↓
UI перерисовывается с переведенным контентом
```

### API Endpoint: /api/translate

**Запрос:**
```json
POST /api/translate
{
  "content": {
    "profession": "Frontend Developer",
    "topics": [
      {
        "title": "Основы React",
        "summary": "Введение в React...",
        "examples": ["Создание компонента", "Работа с state"],
        "tasks": [...],
        "quiz": {...}
      }
    ]
  },
  "targetLanguage": "uz-cyrl"
}
```

**Ответ:**
```json
{
  "translatedContent": {
    "profession": "Frontend дастурчи",
    "topics": [...]
  },
  "debugInfo": [
    "Translation request for language: uz-cyrl",
    "Sending translation request to Gemini...",
    "Successfully parsed translated JSON"
  ]
}
```

### Состояния в lesson/page.tsx

```typescript
const [lessonData, setLessonData] = useState<LessonData | null>(null)
// Текущие данные урока (могут быть переведенными)

const [originalLessonData, setOriginalLessonData] = useState<LessonData | null>(null)
// Оригинальные данные на русском языке (для возврата)

const [translating, setTranslating] = useState(false)
// Флаг процесса перевода (показывает индикатор)

const [translatedLanguage, setTranslatedLanguage] = useState('ru')
// Текущий язык контента (для предотвращения повторного перевода)
```

### useEffect для автоперевода

```typescript
useEffect(() => {
  if (!originalLessonData) return
  if (language === translatedLanguage) return

  translateContent(language)
}, [language, originalLessonData])
```

**Логика:**
1. Проверяем наличие оригинальных данных
2. Сравниваем текущий язык UI с языком контента
3. Если отличаются - запускаем перевод

## Обработка ошибок

### Квота API исчерпана (429)

Система автоматически переключается на следующий API ключ:

```typescript
if (error.message?.includes('429') || error.message?.includes('quota')) {
  debugInfo.push('API quota exceeded, rotating to next key...')
  // Retry with next key
}
```

### Невалидный JSON от API

```typescript
try {
  translatedContent = JSON.parse(text)
} catch (parseError) {
  throw new Error('Invalid JSON response from translator')
}
```

### Сеть недоступна

```typescript
catch (error: any) {
  addLog('ERROR', `Translation error: ${error.message}`)
  // Контент остается на предыдущем языке
}
```

## Логирование

Все действия логируются в LogConsole:

```
[API_REQ] Translating to uz-cyrl
[API_REQ] Sending translation request to Gemini...
[API_RES] Received response from Gemini
[API_RES] Successfully parsed translated JSON
[API_RES] Translation successful
```

При ошибках:

```
[ERROR] Translation error: Failed to fetch
```

## Оптимизация

### Кеширование оригинала

Оригинальные данные хранятся в `originalLessonData` для:
- Быстрого возврата к русскому языку
- Возможности переключения между uz-cyrl и uz-latn без повторного перевода с русского

### Предотвращение лишних запросов

```typescript
if (language === translatedLanguage) return
```

Если контент уже на нужном языке - перевод не выполняется.

### Индикатор загрузки

Пока идет перевод (`translating === true`):
- Показывается индикатор "Перевод..."
- Предотвращается отправка новых запросов
- UI остается интерактивным

## Пример использования

### Сценарий 1: Первый запуск
```
1. Пользователь открывает /lesson
2. Контент загружается на русском (по умолчанию)
3. originalLessonData = данные на русском
4. lessonData = данные на русском
5. translatedLanguage = 'ru'
```

### Сценарий 2: Перевод на узбекский (кириллица)
```
1. Пользователь нажимает на переключатель → выбирает "Ўзбекча"
2. language меняется с 'ru' на 'uz-cyrl'
3. useEffect срабатывает (language !== translatedLanguage)
4. translateContent('uz-cyrl') вызывается
5. translating = true → показывается индикатор
6. Запрос к /api/translate с оригинальными данными
7. Gemini переводит контент
8. lessonData обновляется переведенными данными
9. translatedLanguage = 'uz-cyrl'
10. translating = false → индикатор исчезает
```

### Сценарий 3: Возврат к русскому
```
1. Пользователь выбирает "Русский"
2. language меняется на 'ru'
3. useEffect срабатывает
4. translateContent('ru') НЕ вызывается (можно оптимизировать)
5. Вместо этого: lessonData = originalLessonData
6. Мгновенный возврат к оригиналу (без API запроса)
```

## Дальнейшие улучшения

### 1. Кеширование переводов
```typescript
const [translationCache, setTranslationCache] = useState<{
  'uz-cyrl': LessonData | null,
  'uz-latn': LessonData | null
}>({ 'uz-cyrl': null, 'uz-latn': null })
```

Сохранять переведенные версии для мгновенного переключения.

### 2. Оффлайн режим
```typescript
localStorage.setItem(
  `translation_${profession}_${language}`,
  JSON.stringify(translatedData)
)
```

Сохранять переводы локально для работы без интернета.

### 3. Прогресс-бар перевода
```typescript
<motion.div
  className="progress-bar"
  initial={{ width: 0 }}
  animate={{ width: `${translationProgress}%` }}
/>
```

Показывать прогресс перевода для больших уроков.

## Тестирование

### Ручное тестирование

1. Откройте `/lesson` (после выбора профессии)
2. В sidebar нажмите на переключатель языка
3. Выберите "Ўзбекча" (кириллица)
4. Убедитесь, что:
   - Появился индикатор "Перевод..."
   - Контент обновился на узбекском
   - Логи показывают успешный перевод
5. Переключите на "O'zbekcha" (латиница)
6. Вернитесь на "Русский"

### Проверка логов

Откройте LogConsole (кнопка внизу страницы) и проверьте:
- `[API_REQ] Translating to ...`
- `[API_RES] Translation successful`
- Отсутствие `[ERROR]` сообщений

## Troubleshooting

### Перевод не запускается

**Проблема:** При смене языка ничего не происходит

**Решение:**
1. Проверьте, что LanguageSwitcher присутствует на странице
2. Откройте консоль браузера (F12)
3. Проверьте наличие ошибок JavaScript
4. Убедитесь, что GEMINI_API_KEY установлен в `.env.local`

### Ошибка "Invalid JSON response"

**Проблема:** API возвращает невалидный JSON

**Решение:**
1. Проверьте промпт в `/api/translate/route.ts`
2. Убедитесь, что Gemini возвращает чистый JSON без markdown
3. Проверьте логи API в LogConsole

### Медленный перевод

**Проблема:** Перевод занимает > 10 секунд

**Решение:**
1. Используйте `gemini-2.0-flash-exp` (быстрее чем pro)
2. Уменьшите объем переводимого контента
3. Реализуйте кеширование переводов

### Перевод не сохраняется

**Проблема:** После перезагрузки страницы контент снова на русском

**Ожидаемое поведение:** Это нормально! Переводы не сохраняются.

**Решение (если нужно):** Реализуйте сохранение в localStorage (см. раздел "Дальнейшие улучшения")
